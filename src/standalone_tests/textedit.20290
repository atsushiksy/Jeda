main() {
  string s ;
  
  if( s ) error( "s returns true\n" ) ;
  else printf( "s returns false\n" ) ;
  
  if( s == null ) printf( "s is null!!\n" ) ;
  else error( "ERROR s is not null!!\n" ) ;
  
  if( s == "" ) error( "ERROR s is \"\"\n" ) ;
  else printf( "s is not \"\"\n" ) ;
  
  if( null == s ) printf( "s is null!!\n" ) ;
  else error( "ERROR s is not null!!\n" ) ;
  
  if( "" == s ) error( "ERROR s is \"\"\n" ) ;
  else printf( "s is not \"\"\n" ) ;
  
  s = "" ;
  if( s ) error( "ERROR s returns true\n" ) ;
  else printf( "s returns false\n" ) ;
  
  if( s == null ) error( "ERROR s is null!!\n" ) ;
  else printf( "s is not null!!\n" ) ;
  
  if( s == "" ) printf( "s is \"\"\n" ) ;
  else error( "s is not \"\"\n" ) ;

  if( null == s ) error( "ERROR s is null!!\n" ) ;
  else printf( "s is not null!!\n" ) ;
  
  if( "" == s ) printf( "s is \"\"\n" ) ;
  else error( "ERROR s is not \"\"\n" ) ;

}
